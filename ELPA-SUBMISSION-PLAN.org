#+TITLE: ELOT MELPA Submission — Step-by-Step Implementation Plan
#+AUTHOR: ELOT Contributors
#+DATE: 2026
#+STARTUP: overview
#+PROPERTY: header-args :eval never-export

* Purpose and how to use this document

This document is the working plan for getting ELOT (=elot-package/=)
into shape for submission to MELPA.  It is structured for incremental
execution with LLM assistance through Emacs ~gptel~:

- Each *Milestone* is a self-contained unit of work.  Open the
  milestone heading, narrow to it (=C-x n s=), and send the whole
  subtree to ~gptel~ as the prompt.
- Each milestone lists *Files affected* and *Steps* as concrete
  verbs ("edit X to do Y") — not pseudocode, not snippets — so
  gptel has unambiguous targets.
- Each milestone ends with an *Acceptance check*: a short list of
  shell or Emacs commands whose output proves the milestone is done.
  After every milestone the package must remain in a *stable status*:
  byte-compiles cleanly, the existing ERT suite passes, =elot-mode=
  still loads and enables in an example file.
- Milestones are ordered to minimise rework.  The SPARQL rewrite
  comes first (it changes the shape of code that later milestones
  touch), then legacy cleanup, then metadata polish, then CI and
  the recipe.

Conventions used below:
- =MAIN FILE= refers to the file declared as the package's
  top-level descriptor.  *The MAIN FILE is
  =elot-package/elot.el=* and the MELPA package name is =elot=.
  This matches MELPA's convention that, for a recipe named
  =elot=, the file =elot.el= is treated as the main library.
  In addition to carrying the canonical headers, =elot.el= is the
  single load entry point: it ~require~s the other =elot-*.el=
  modules so that a user only needs =(require 'elot)= (or
  ~load-library~ "elot") to make =elot-mode=, =elot-tangle= and the
  rest available.
- "Stable status" is the green checkpoint after each milestone:
  see [[*Stable-status checklist (run after every milestone)][Stable-status checklist]].

* Stable-status checklist (run after every milestone)
:PROPERTIES:
:CUSTOM_ID: stable-status
:END:

After completing any milestone, all of the following must hold.  If
any item fails, do not start the next milestone.

All three checks below are wired into the top-level =Makefile=.
The one-shot command is:

#+begin_src sh
make stable-check
#+end_src

This runs byte-compile, the ERT suite, and the load smoke test in
sequence, writes per-step logs under =tmp/stable-check/=, and prints
a one-line =PASS= / =FAIL= summary.  Use the individual targets
below when iterating on a single check.

1. *Byte-compile is clean* — =make byte-compile= (or, equivalently,
   from the repository root):
   #+begin_src sh
   cd elot-package
   emacs --batch -L . \
     --eval "(setq byte-compile-error-on-warn nil)" \
     -f batch-byte-compile *.el
   #+end_src
   No errors.  Warnings should not increase relative to the previous
   stable point (record the baseline count once at the start; see
   Step B.1 / =make baseline-warnings=).

2. *Tests pass* — =make test= (which delegates to =make -C test=):
   #+begin_src sh
   make -C test
   #+end_src
   All ERT tests green (ROBOT-gated tests may skip; that is
   acceptable and expected on machines without ROBOT).

3. *Loads in a fresh Emacs* — =make smoke=:
   #+begin_src sh
   emacs --batch -L elot-package \
     --eval "(require 'elot-mode)" \
     --eval "(message \"OK: elot-mode loaded\")"
   #+end_src
   Note: until [[#milestone-4-package-requires][Milestone 4]] wires =elot.el= up as the canonical
   load entry point, the smoke test requires =elot-mode= directly.
   From Milestone 4 onwards (specifically Step 4.6), the smoke test
   switches to =(require 'elot)= and additionally asserts that
   =elot-mode= is bound (autoloaded) as a side effect.  The
   =make smoke= target in the top-level =Makefile= is updated at
   that point.

4. *Working tree clean* — commit the milestone before moving on:
   =git status= shows no uncommitted changes after the commit.

* DONE Step 0 — Create a working branch for ELPA submission prep
CLOSED: [2026-05-04 man. 11:32]
:PROPERTIES:
:CUSTOM_ID: working-branch
:END:

All work in this plan — including the top-level =Makefile= introduced
in the [[#baseline][Baseline]] milestone — should land on a dedicated branch
rather than directly on =main=.  This keeps the submission-prep work
isolated, easy to review, and trivial to abandon or rebase if MELPA
review prompts a redesign.

** Recommended branch name

=elpa-submission= (or, equivalently, =melpa-submission=).  The name
should make it obvious to a future reader why the branch exists.

** Steps

1. Confirm the working tree is clean and you are on =main= (or
   whatever branch you want to fork from):
   #+begin_src sh
   git status
   git switch main
   git pull --ff-only
   #+end_src

2. Create and switch to the new branch:
   #+begin_src sh
   git switch -c elpa-submission
   #+end_src

3. Verify:
   #+begin_src sh
   git branch --show-current   # → elpa-submission
   #+end_src

4. Now make the first commit on this branch — the top-level
   =Makefile= introduced in the Baseline milestone:
   #+begin_src sh
   git add Makefile
   git commit -m "Add top-level Makefile for ELPA submission prep"
   #+end_src

5. (Optional) Push the branch and set upstream so subsequent
   =git push= calls have somewhere to go:
   #+begin_src sh
   git push -u origin elpa-submission
   #+end_src

** Acceptance check
- =git branch --show-current= prints =elpa-submission=.
- =git log --oneline main..HEAD= shows the Makefile commit (and only
  that, at this point).
- =git status= is clean.

* DONE Baseline: capture the starting point
CLOSED: [2026-05-04 man. 11:39]
:PROPERTIES:
:CUSTOM_ID: baseline
:END:

Before touching anything, record the current state so regressions
are obvious.  This milestone produces no code changes.

All three steps are automated by the top-level =Makefile=:

#+begin_src sh
make baseline
#+end_src

This is equivalent to running =make baseline-warnings=,
=make baseline-tests= and =make baseline-symbols= in sequence.  By
default, files are written under =/tmp/=; override with
=make baseline BASELINE_DIR=/some/where=.

** DONE Step B.1 — Record byte-compile warning baseline
CLOSED: [2026-05-04 man. 11:38]
- =make baseline-warnings= (or run the byte-compile command from
  the [[#stable-status][stable-status checklist]] manually).
- Saves the warning count and the full warning text to
  =/tmp/elot-baseline-warnings.txt=.

** DONE Step B.2 — Record passing test list
CLOSED: [2026-05-04 man. 11:38]
- =make baseline-tests= (equivalent to =make -C test= with output
  captured).
- Saves the list of passed tests to =/tmp/elot-baseline-tests.txt=.

** DONE Step B.3 — Record current loaded symbols
CLOSED: [2026-05-04 man. 11:38]
- =make baseline-symbols=.
- For each file in =elot-package/*.el=, lists the public defuns/
  defvars/defcustoms.  Saved to =/tmp/elot-baseline-symbols.txt=.
- This becomes the reference for "did I accidentally remove a public
  API?" checks during *Milestone 2* (the legacy LOB removal — that
  is where most public symbols disappear).

** Acceptance check
- Three baseline files exist under =/tmp/= (or under =BASELINE_DIR=).
- No code changes; =git status= shows only the new top-level
  =Makefile= (committed as part of this baseline step) — nothing
  else.

Result:
#+begin_example
$ make baseline
==> Capturing byte-compile baseline -> /tmp/elot-baseline-warnings.txt
    warnings: 50  errors: 1
==> Capturing test baseline -> /tmp/elot-baseline-tests.txt
    passed: 0  failed: 0
==> Capturing symbol baseline -> /tmp/elot-baseline-symbols.txt
    public symbols (approx): 308
Baseline written:
  /tmp/elot-baseline-warnings.txt
  /tmp/elot-baseline-tests.txt
  /tmp/elot-baseline-symbols.txt
#+end_example

* Milestone 1 — Rewrite SPARQL support
:PROPERTIES:
:CUSTOM_ID: milestone-1-sparql
:END:

** Background

ELOT customises Org's ~org-babel-execute:sparql~ (defined in
=elot.el= ≈ line 513, installed by =advice-add= ≈ line 545) to:

- inject prefix declarations from =org-link-abbrev-alist-local=
  into every query, and
- support querying local RDF/OWL files via ROBOT — which is the
  most common ELOT workflow: querying the ontology you are
  currently editing.

The current implementation has three weaknesses that should be
fixed *before* the rest of the submission prep, because subsequent
milestones (legacy LOB removal, mode lifecycle wiring, header
polish) all touch this code:

1. The ELOT advice builds the prefix block from
   =org-link-abbrev-alist-local= via =elot-prefix-block-from-alist=
   (=elot.el= ≈ line 530). A query body without any PREFIX boilerplate
   must execute correctly. (This does seem to work OK already.)

2. *Robustness gaps.*  The advice does not gracefully handle:
   - missing or unreachable =:url= argument,
   - non-existent local files passed as =:url=,
   - non-zero ROBOT exit, with stderr lost,
   - empty result sets (the ob-sparql table converter throws),
   - non-ASCII characters in queries.

3. *Global side effect.*  =advice-add= is installed at top level,
   so the ELOT-specific behaviour is active in every Emacs session
   that has loaded ELOT, even in non-ELOT buffers.  The advice body
   does guard on =(elot--is-elot-buffer)=, but MELPA still
   discourages global advice installed at =require= time.  This
   item overlaps with [[#milestone-3-side-effects][Milestone 3]] Step 3.3 — the advice-move work
   is done *here* (Step 1.5) and Milestone 3 only needs to confirm
   it.

The relevant code is concentrated in:
- =elot-package/elot.el= (≈ lines 460–546): prefix-block builder
  =elot-prefix-block-from-alist= and the advice
  =elot--custom-org-babel-execute-sparql=.
- =elot-package/elot-label-display.el= (≈ lines 170–250): label
  retrieval helpers that build SPARQL queries directly.
- =elot-package/elot-mode.el= (≈ lines 280–300): tempo templates
  =elot-block-sparql-select= and =elot-block-sparql-construct=.

** DONE Step 1.1 — Inventory current SPARQL code paths
- *Files*: =elot-package/elot.el=, =elot-package/elot-label-display.el=,
  =elot-package/elot-mode.el=.
- Produce a short note (commit it as a comment block in this plan) listing:
  - every function that reads =org-link-abbrev-alist-local=,
  - every call site of =elot-prefix-block-from-alist=,
  - every place that assembles a SPARQL query string,
  - every example file that contains sparql queries
    (~#+begin_src sparql~)
- This inventory is the reference for the rewrite and for the
  acceptance check.

*** Inventory result

**** Reads of =org-link-abbrev-alist-local=

| File (tangled)                     | Line | Function                              | Literate source                                           |
|------------------------------------+------+---------------------------------------+-----------------------------------------------------------|
| =elot-package/elot.el=               |  513 | =elot--custom-org-babel-execute-sparql= | =elot-defs.org= block =src-sparql-exec-patch= (from line 600) |
| =elot-package/elot-label-display.el= |   40 | (=defvar= forward declaration only)     | #ERROR                                                    |
| =elot-package/elot-label-display.el= |  173 | =elot-label-attribs-query=              | =elot-label-display.org= (line 262)                         |

No other production files read =org-link-abbrev-alist-local=.

**** Call sites of =elot-prefix-block-from-alist=

Definition: =elot-package/elot.el= ≈ line 464 (tangled from
=elot-defs.org= named block =src-prefix-blocks=, ≈ line 540).
Signature: =(elot-prefix-block-from-alist PREFIXES FORMAT)= where
=FORMAT= is one of =omn=, =sparql=, =ttl=.

| File                                    | Line | Caller                                  | Format arg | Notes                                |
|-----------------------------------------+------+-----------------------------------------+------------+--------------------------------------|
| =elot-package/elot.el=                  |  530 | =elot--custom-org-babel-execute-sparql= | =sparql=   | Built into =elot-prefixed-query= and used on both branches: the ROBOT branch (local file =:url=) and the endpoint branch (=http(s)= =:url=) now both receive the prefix-injected query. |
| =elot-package/elot-label-display.el=    |  173 | =elot-label-attribs-query=              | =sparql=   | Concatenated ahead of a hand-written PREFIX block (rdf, rdfs, owl, skos, dcterms, iof-av) and the =SELECT= body. |
| =examples/fdis.org=                     | 1649 | named block =sparql-prefixes=           | =sparql=   | Wrapped as a noweb call =<<sparql-prefixes()>>= and inlined into queries at lines 1848 and 1896. |
| =examples/fdis.org=                     | 1735 | inline emacs-lisp call (header-args :var) | =sparql= | Passed as =:var prefixes=… into a query block. |
| =examples/reasoner-example.org=         |  413 | named block =sparql-prefixes=           | =sparql=   | Same noweb shape as fdis.org; not actually expanded into any query block in this file (the file's blocks rely on =:noweb yes= header default but no =<<sparql-prefixes()>>= reference appears). |

No production code calls =elot-prefix-block-from-alist= with
=omn= or =ttl= as the format argument; only =sparql= is exercised
in the live code paths.

**** Places that assemble a SPARQL query string

In =elot-package/elot.el=:
- =elot-robot-execute-query= (≈ line 490) — writes a query string
  to a temp =.sparql= file and shells out to ROBOT.  Does *not*
  itself prepend prefixes; relies on caller to have done so.
- =elot--custom-org-babel-execute-sparql= (≈ line 513) — assembles
  =elot-prefixed-query= from the prefix block + the
  =org-babel-expand-body:sparql=–expanded body, and sends that
  prefixed string to both backends: =elot-robot-execute-query= on
  the ROBOT branch (local file =:url=) and =sparql-execute-query=
  on the endpoint branch (=http(s)= =:url=).  Prefix injection is
  now symmetric across the two branches.

In =elot-package/elot-label-display.el=:
- =elot-label-attribs-query= (≈ line 169) — concatenates
  =elot-prefix-block-from-alist= output with a hard-coded PREFIX
  block (rdf, rdfs, owl, skos, dcterms, iof-av) and a =SELECT=
  body that pulls id / label / attribute plist; supports optional
  filter and limit fragments.
- =elot-retrieve-prefixes= (≈ line 199) — uses a fixed string
  =construct where {?x ?y ?z} limit 0= to harvest =@prefix=
  declarations from a TTL response.
- =elot-retrieve-labels-plist= (≈ line 228) — calls
  =elot-label-attribs-query= and dispatches to
  =sparql-execute-query= (endpoints only; raises =error= for
  local-file URLs).

In =elot-package/elot-mode.el=:
- =tempo-define-template "elot-block-sparql-select"= (≈ line 280)
  — inserts a =#+begin_src sparql :url "<context>.omn" ...= block
  with a bare =select { }= body (no inline PREFIX, no noweb call).
- =tempo-define-template "elot-block-sparql-construct"=
  (≈ line 295) — inserts a =#+begin_src sparql :url
  "<context>.omn" :format ttl :wrap "src ttl" :cache yes :post
  kill-prefixes(data=*this*) ...= block with a bare =construct {
  } { }= body (also no inline PREFIX, no noweb call).

The tempo templates are *already* clean — Step 1.3 will only
need to verify them and update the surrounding documentation.

**** Example files containing =#+begin_src sparql= (org-babel source blocks)

| File                          | Blocks | Inline =PREFIX= boilerplate? | Noweb prefix injection?                                                                                                  |
|-------------------------------+--------+----------------------------+--------------------------------------------------------------------------------------------------------------------------|
| =examples/reasoner-example.org= |      6 | Yes (in some blocks)       | Defines named block =sparql-prefixes= but no expansion is wired in via =<<...>>= references in the SPARQL blocks themselves. |
| =examples/fdis.org=             |      4 | No                         | Yes — =<<sparql-prefixes()>>= at lines 1848 and 1896, plus a =:var prefixes=…= header-args mechanism at line 1735.           |

No other =examples/*.org= file contains =#+begin_src sparql=.

**** Top-level side effect

=elot-package/elot.el= line 545 installs the SPARQL advice at
=require= time:

: (advice-add 'org-babel-execute:sparql :around #'elot--custom-org-babel-execute-sparql)

Tangled from =elot-defs.org= ≈ line 638.  This is what Step 1.5
moves into =elot-mode--enable= / =elot-mode--disable=.

**** Asymmetries surfaced by the inventory (input to Step 1.2 / 1.4)

1. *(Resolved.)*  Previously the endpoint branch
   (=string-match-p "^http" url=) sent the *unprefixed* =query=
   while the ROBOT branch sent =elot-prefixed-query=.  The advice
   has been updated so both branches now send
   =elot-prefixed-query=, giving consistent prefix injection
   regardless of whether =:url= points at a local file or an HTTP
   endpoint.  See the patched =elot--custom-org-babel-execute-sparql=
   under Step 1.2.
2. =elot-label-attribs-query= injects prefixes from
   =org-link-abbrev-alist-local= *and* hard-codes a second PREFIX
   block.  After Step 1.2 lands de-duplication, this hand-rolled
   block can be reduced to whatever subset is not already in
   =org-link-abbrev-alist-local= (or removed entirely if every
   prefix it lists is already standard in ELOT files).
3. The =:format= header arg drives =format-symbol= in the ROBOT
   branch via a string match on =turtle\|ttl=; anything else
   becomes =csv=.  An empty/absent =:format= silently becomes
   =csv= — Step 1.4 should make this explicit.
4. =elot-retrieve-labels-plist= raises =error= for local-file
   URLs ("ROBOT ontology-file query not implemented yet for elot
   labels query") — pre-existing limitation, noted but out of
   scope for Milestone 1.

** DONE Step 1.2 — De-duplicate prefix declarations
CLOSED: [2026-05-06 ons. 23:15]
- *File*: =elot-package/elot.el=.
- Context: =elot--custom-org-babel-execute-sparql= already
  unconditionally prepends the prefix block built from
  =org-link-abbrev-alist-local= to the query body, regardless of
  whether the body contains its own inline =PREFIX= lines.  The
  earlier note in this plan (claiming the prefix block was only
  prepended when no inline =PREFIX= lines were present) was
  incorrect — confirmed by reading =elot.el= ≈ lines 522–530.
  Concatenation today therefore yields *duplicate* =PREFIX=
  declarations when the body already declares the same label,
  which most SPARQL engines tolerate but ROBOT and some endpoints
  reject (or warn about) when the URIs disagree.
- Change the prefix handling so the merged query has *exactly one*
  declaration per prefix label, with the following semantics:
  1. Build the merged sequence of declarations as
     =(injected-from-alist ++ inline-from-body)=, preserving
     source order within each part.  "Inline" means a top-of-body
     =PREFIX foo: <uri>= line as recognised by SPARQL 1.1 syntax
     (case-insensitive keyword, optional surrounding whitespace).
  2. Walk the merged sequence and collapse by prefix label such
     that *the last declaration wins*.  Because the alist-derived
     declarations come first, this means an inline declaration in
     the user's body always overrides one injected from
     =org-link-abbrev-alist-local= — i.e. the user can override
     ELOT's project-wide prefix mapping by writing =PREFIX foo:
     <other-uri>= at the top of the block.
  3. If two declarations share a label but disagree on the URI,
     emit a single =display-warning= (category =elot-sparql=,
     level =:warning=) naming the label, both URIs, and which
     one wins ("last").  Identical re-declarations (same label
     and same URI) are silently de-duplicated.
  4. Strip the now-redundant inline =PREFIX= lines from the body
     before re-assembling the query, so the final string contains
     each prefix exactly once at the top.
- Factor the merge/dedup logic into a pure helper, e.g.
  =elot--sparql-merge-prefixes (alist body) → (prefix-block
  . body-without-prefix-lines)=, so it can be unit-tested without
  invoking ROBOT or any endpoint.
- Goal (unchanged): a query block containing only the
  user-written =SELECT= / =CONSTRUCT= body — no =PREFIX=
  boilerplate, no noweb call — executes correctly.  *Additional*
  goal from this step: a query block that *does* carry inline
  =PREFIX= lines (legacy or override) executes without producing
  duplicate declarations, and conflicting overrides are surfaced
  as warnings rather than silently shadowed.
- Add ERT tests under =test/= for the helper covering: empty
  body; body with no inline prefixes; body with inline prefixes
  fully covered by the alist (identical URIs → silent dedup);
  body with inline prefixes that override the alist (different
  URIs → warning, last wins); body with inline-only prefixes not
  in the alist (preserved verbatim).  These tests do not require
  ROBOT.  A separate end-to-end test that actually executes a
  query through the advice should be gated with =skip-unless=
  on ROBOT availability.

** DONE Step 1.3 — Update tempo templates
CLOSED: [2026-05-06 ons. 23:15]

Verification only: a re-read of =elot-package/elot-mode.el=
confirmed that =elot-block-sparql-select= and
=elot-block-sparql-construct= already insert clean query bodies
— no =<<resource-declarations(...)>>= noweb reference, no
hand-rolled =PREFIX= boilerplate.  The Step 1.1 inventory had
already noted this; Step 1.3 therefore requires no code change.

One item flagged for follow-up, *out of scope for Milestone 1*:
=elot-block-sparql-construct= carries
: :post kill-prefixes(data=*this*)
This is a deliberate convenience — =kill-prefixes= strips =@prefix=
declarations from the CONSTRUCT result for readability — and it
stays.  However, =kill-prefixes= is currently implemented as a
Library-of-Babel block in =elot-package/elot-lob.org= (see
[[file:elot-package/elot-lob.org::kill-prefixes][kill-prefixes]]),
which is the very pipeline being retired in [[#milestone-2-legacy-cleanup][Milestone 2]].  Before
=elot-lob.org= is deleted (or even just left inert), =kill-prefixes=
must be reimplemented as a regular Elisp function so the
=:post= hook keeps working.  Tracked under Milestone 2 Step 2.5.

No documentation snippets in =elot-mode.org= or =examples/=
reference the old (prefix-laden, noweb-injected) tempo template
shape, so nothing to update there.

** DONE Step 1.4 — Harden =elot--custom-org-babel-execute-sparql=
- *File*: =elot-package/elot.el= (mirrored in =elot-defs.org=
  blocks =src-robot-query= and =src-sparql-exec-patch=; see
  Step 1.7 for the formal tangle round-trip check).

The advice body and =elot-robot-execute-query= were factored into
named helpers so each failure mode is exercised by a unit test
without standing up ROBOT.  The new helpers, all in =elot.el=:

| Helper                              | Responsibility                                                                                              |
|-------------------------------------+-------------------------------------------------------------------------------------------------------------|
| =elot--sparql-classify-url=         | Validates =:url=.  Returns the symbol =endpoint= or =local-file=, or signals a =user-error=.                |
| =elot--sparql-resolve-format=       | Normalises =:format= to the symbol =csv= or =ttl=.  Warns (=display-warning= category =elot-sparql=) on unrecognised non-empty values; nil/empty silently default to =csv=. |
| =elot--sparql-result-empty-p=       | Detects an empty result buffer (totally empty, whitespace-only, or — for =csv= — header-row-only).          |
| =elot--sparql-empty-result-sentinel= | Constant string returned in place of an empty CSV body so =org-babel-sparql-convert-to-table= is not invoked on input it cannot parse. |

Failure modes addressed (one-to-one with the original checklist):

1. *=:url= not provided.*  =elot--sparql-classify-url= signals a
   =user-error= naming the missing parameter and explaining what
   to set it to.  Also catches non-string =:url= values.
2. *Non-existent local file.*  The same helper resolves =:url=
   via =expand-file-name= and signals a =user-error= that names
   both the resolved absolute path and the current
   =default-directory=.  =elot-robot-execute-query= performs the
   same check defensively as a second line of defence, and
   additionally errors out if =elot-robot-jar-path= is unset or
   missing.
3. *ROBOT non-zero exit.*  =elot-robot-execute-query= no longer
   shells out via =elot-robot-command= (which uses
   =shell-command= and discards stderr).  It now uses
   =call-process= directly with stderr captured to a temp file,
   and on non-zero exit raises =error= whose message includes the
   exit code and ROBOT's trimmed stderr.  The temp file is
   cleaned up via =unwind-protect=.
4. *Empty result set.*  The advice now consults
   =elot--sparql-result-empty-p= before calling
   =org-babel-sparql-convert-to-table=.  An empty CSV result
   yields =elot--sparql-empty-result-sentinel= ("[empty result
   set]") instead of a backtrace.
5. *Non-ASCII / multibyte content.*  Both the advice body and
   =elot-robot-execute-query= bind =coding-system-for-read= and
   =coding-system-for-write= to =utf-8= around their
   =with-temp-buffer= / =with-temp-file= forms.  The Java
   sub-process additionally receives =-Dfile.encoding=UTF-8= so
   its parsing of the query file and writing of the result file
   match.  The query temp file's buffer-file-coding-system is
   pinned to =utf-8= explicitly.

Other shape changes worth noting:

- The advice now branches on =elot--sparql-classify-url='s
  return value via =pcase= rather than re-doing a
  =string-match-p "^http"= check, so the endpoint vs. local-file
  distinction is made exactly once.
- Both branches now receive the same fully-prefixed query
  (asymmetry resolved in Step 1.2; preserved here).
- The diagnostic =message= at entry now reports the classified
  =kind= and resolved =format-symbol= rather than the bare
  "Executing a SPARQL query block …" line — useful when
  diagnosing why ROBOT was (or was not) invoked.

*** Tests added

New file: =test/elot-sparql-exec-helpers-test.el= — pure-helper
ERT suite for the three new helpers.  Coverage:

- =elot--sparql-resolve-format=: nil / empty default silently to
  =csv=; =ttl=, =turtle=, =text/turtle= → =ttl=; =csv=, =text/csv=
  → =csv=; unknown non-empty values warn and default to =csv=.
- =elot--sparql-classify-url=: nil, empty, and non-string =:url=
  raise =user-error=; =http://= and =https://= → =endpoint=;
  existing local file → =local-file=; missing local file raises
  =user-error= mentioning the resolved absolute path.
- =elot--sparql-result-empty-p=: empty buffer, whitespace-only,
  CSV header-row-only, CSV with rows, TTL with content — all
  classified correctly under both =csv= and =ttl= modes.

The tests do not require ROBOT and do not stand up an HTTP
endpoint.  Wired into =test/Makefile= as the
=sparql-exec-helpers-test= target and added to the default =test=
target.

A separate end-to-end test that actually executes a query
through the advice is left for Step 1.6 (regression on a real
example file) and remains gated on ROBOT availability.

** TODO Step 1.5 — Localise the advice to ELOT buffers
- *Files*: =elot-package/elot.el=, =elot-package/elot-mode.el=.
- Remove the top-level
  =(advice-add 'org-babel-execute:sparql :around #'elot--custom-org-babel-execute-sparql)=
  call (=elot.el= ≈ line 545).
- Install the advice from =elot-mode--enable= and remove it from
  =elot-mode--disable=, guarded by a module-level boolean
  =elot--sparql-advice-installed-p= so multiple ELOT buffers share
  a single installation and disabling the mode in the *last* such
  buffer fully uninstalls it.
- The advice body's existing =(elot--is-elot-buffer)= guard stays
  as defence in depth: if some other code path enables the advice,
  it still no-ops outside ELOT buffers.
- This step satisfies Milestone 3 Step 3.3 in advance; that step
  is reduced to verification only.

** TODO Step 1.6 — Regression test on real files
- Pick an =examples/*.org= file that exercises both =SELECT= and
  =CONSTRUCT= against a local =.omn= via ROBOT.
- Strip every embedded =PREFIX= boilerplate and every
  =<<resource-declarations(...)>>= noweb call from its SPARQL
  blocks.
- Run the queries before and after the rewrite (use a saved copy
  of =main= for "before").  Differences must be limited to:
  - identical results, now delivered without the embedded prefix
    block;
  - clearer error messages on the failure paths from Step 1.4.
- Capture the comparison transcript in a =#+begin_example= block
  under this milestone heading.

** TODO Step 1.7 — Mirror in literate sources
- *Files*: =elot-defs.org=, =elot-mode.org=.
- Update the source blocks for every function/template/advice
  touched above so that =org-babel-tangle= reproduces the new
  =.el= contents exactly.  =git diff= after a clean tangle must
  show no further changes.

** Acceptance check
- An =examples/*.org= file with a SPARQL block containing *no*
  =PREFIX= declarations and *no* =resource-declarations= noweb
  reference executes successfully against a local =.omn= via
  ROBOT and against a remote endpoint.
- In a fresh Emacs, =(require 'elot)= followed by
  =(advice--p (advice--symbol-function 'org-babel-execute:sparql))=
  returns =nil=.  After =M-x elot-mode= in an ELOT org buffer it
  returns non-nil.  After disabling =elot-mode= in the last
  remaining ELOT buffer it returns =nil= again.
- =org-babel-execute:sparql= invoked from a non-ELOT org buffer
  behaves identically to upstream =ob-sparql= (no ELOT-specific
  prefix injection, no ROBOT path).
- Run the [[#stable-status][stable-status checklist]].

* Milestone 2 — Resolve legacy =elot.el= LOB pipeline
:PROPERTIES:
:CUSTOM_ID: milestone-2-legacy-cleanup
:END:

** Background

=elot-package/elot.el= still contains ~20 functions left over from
the pre-rewrite "Library of Babel" (LOB) pipeline.  They are no
longer used by =elot-tangle.el=, but until Milestone 1 they were
also reachable through:
- =elot-package/elot-lob.org=, whose named blocks
  =resource-declarations= and =resource-taxonomy= called
  =elot-resource-declarations-from-header= and
  =elot-resource-taxonomy-from-header=; and
- example files that invoke those named blocks via
  =<<resource-declarations(...)>>= noweb references.

After Milestone 1 the SPARQL path no longer needs the LOB prefix
block at all.  The remaining LOB calls in example files are
*silently ignored* by the new tangle pipeline: an old file that
contains =<<resource-declarations(...)>>= still exports correctly
to OWL because the new pipeline does not look at those noweb
calls.  This means the LOB functions can be deleted outright
without breaking existing ontology sources.

One legacy function — =elot-prefix-block-from-alist= — is still
called from production code in =elot-label-display.el= (line ≈173)
and (after Milestone 1) from the rewritten SPARQL advice.  It must
be preserved (relocated, not deleted).

** Decision: Strategy B (remove and migrate)

We delete the legacy LOB functions.  Rationale:

- After Milestone 1, no production code path depends on them.
- Old example files that still contain LOB noweb calls continue
  to export to OWL correctly via the new tangle pipeline (those
  calls are silently ignored).
- Only one example, =examples/reasoner-example.org=, contains the
  old SPARQL prefix boilerplate.  It is a special-case file and
  is acceptable to leave as-is — it does not gate the milestone.
- This avoids carrying ~20 dead functions plus a deprecation
  schedule into the MELPA submission.

** TODO Step 2.1 — Move =elot-prefix-block-from-alist= to =elot-tangle.el=
- *Files*: =elot-package/elot-tangle.el=, =elot-package/elot.el=.
- Cut the =defun elot-prefix-block-from-alist= block (currently in
  =elot.el= around line 464) and paste it into =elot-tangle.el=
  near the other prefix helpers (after =elot-update-link-abbrev=).
- =elot-tangle.el= is authored directly (no noweb tangle markers
  needed).  Keep that style.
- Remove the function from =elot.el=.
- Update the call sites in =elot-label-display.el= and the
  rewritten SPARQL advice (=elot.el= itself, post-Milestone 1) so
  they pick up the relocated definition — both files already
  ~require~ =elot-tangle= transitively, so no new =require= is
  needed; verify with byte-compile.

** TODO Step 2.2 — Mirror the move in =elot-defs.org=
- *File*: =elot-defs.org=.
- Find the named block =src-prefix-blocks= and either:
  - delete it (if the function now lives only in =elot-tangle.el=
    via direct authoring — preferred), or
  - retarget its =:tangle= header to
    =./elot-package/elot-tangle.el=.
- Verify with =M-x org-babel-tangle= on =elot-defs.org=: the
  produced =elot.el= no longer contains the function; =git diff=
  shows no further changes.

** TODO Step 2.3 — Delete the legacy LOB functions from =elot.el=
- *File*: =elot-package/elot.el=.
- Delete each of the following function definitions outright:

  - =elot-org-descriptions-in-section-helper=
  - =elot-org-descriptions-in-section=
  - =elot-org-subsection-descriptions=
  - =elot-org-list-siblings=
  - =elot-omn-declare=
  - =elot-annotation-entries=
  - =elot-restriction-entries=
  - =elot-omn-annotate=
  - =elot-omn-restrict=
  - =elot-resource-declarations=
  - =elot-format-misc-axiom-annotations=
  - =elot-misc-axioms=
  - =elot-resource-declarations-from-header=
  - =elot-class-oneof-from-header=
  - =elot-class-disjoint-from-header=
  - =elot-org-tags-in-string=
  - =elot-resource-taxonomy-from-l=
  - =elot-resource-taxonomy-from-header=

- Remove any =;; [[file:../elot-defs.org::...]]= noweb tangle
  markers that bracketed the deleted blocks.
- Confirm with =git grep= that no production code references any
  of the deleted symbols.

** TODO Step 2.4 — Remove corresponding entries from =elot-defs.org=
- *File*: =elot-defs.org=.
- Delete every named source block that tangled into the now-
  removed functions in =elot.el=.
- Re-tangle and verify =git diff elot-package/elot.el= shows no
  further changes.

** TODO Step 2.5 — Leave =elot-package/elot-lob.org= and old example files alone
- *Prerequisite*: reimplement =kill-prefixes= as a regular Elisp
  function (e.g. in =elot-tangle.el= or a small new
  =elot-sparql-post.el=) before doing anything else in this step.
  =kill-prefixes= is currently a Library-of-Babel block in
  =elot-lob.org= and is referenced from
  =elot-block-sparql-construct= as =:post kill-prefixes(data=*this*)=
  (see Milestone 1 Step 1.3).  As long as the implementation
  lives in =elot-lob.org=, that file cannot be safely retired.
  Update the tempo template's =:post= hook to call the new Elisp
  function, then proceed with the rest of this step.
- =elot-lob.org= is no longer wired into anything: its named
  blocks may be left in place (they are inert) or deleted.
  Recommendation: leave them, with a top-of-file comment noting
  that the file is retained only so old example files can still
  be opened without =org-babel-tangle= raising "named block not
  found" errors.  If you do delete it, also remove the entry from
  the MELPA recipe =:files= list (Milestone 7 Step 7.1).
- =examples/reasoner-example.org= contains the old SPARQL prefix
  boilerplate.  It is a special-case file; leave it as-is.  If a
  user runs its SPARQL blocks they will simply get the embedded
  PREFIX lines plus the ELOT-injected ones (de-duplication from
  Milestone 1 Step 1.2 covers this cleanly).
- Other =examples/*.org= files that contain
  =<<resource-declarations(...)>>= or
  =<<resource-taxonomy(...)>>= noweb calls: leave them.  The new
  tangle pipeline silently ignores those calls and produces the
  correct =.omn=.  Verify by tangling
  =examples/pizza/pizza.org= (or any other current example) and
  confirming the output is byte-identical to a copy saved before
  the change (=diff -u before.omn after.omn=).

** TODO Step 2.6 — Update =REFACTORING.org= Design Decisions
- *File*: =REFACTORING.org=.
- Add an entry under "Design Decisions" recording: Strategy B
  was chosen — the LOB pipeline functions were removed outright
  rather than deprecated, because the SPARQL rewrite (Milestone
  1) eliminated the last production dependency on them and old
  example files continue to tangle correctly.

** Acceptance check
- =git grep -n "elot-resource-declarations-from-header\|elot-resource-taxonomy-from-header\|elot-org-list-siblings\|elot-org-subsection-descriptions"= returns no results inside =elot-package/=.
- =git grep -n "elot-prefix-block-from-alist" elot-package/= shows
  it defined exactly once, in =elot-tangle.el=, and called from
  =elot-label-display.el= and the rewritten SPARQL advice.
- Tangle a current =examples/*.org= and confirm the resulting
  =.omn= is byte-identical to a copy saved before the change.
- Run the [[#stable-status][stable-status checklist]] — *all green*.

* Milestone 3 — Remove load-time global side effects
:PROPERTIES:
:CUSTOM_ID: milestone-3-side-effects
:END:

** Background

ELPA/MELPA strongly discourage packages that mutate global state
during =require=.  ELOT currently does this in four places in
=elot.el= and three in =elot-mode.el=.  The fix is to move each
side effect into the =elot-mode= enable/disable lifecycle so it
only takes effect for buffers that have opted in.

The SPARQL =advice-add= is already handled in [[#milestone-1-sparql][Milestone 1]] Step
1.5; this milestone only needs to verify it.

** TODO Step 3.1 — Replace bare =setq= for =elot-missing-dependencies=
- *File*: =elot-package/elot.el= (≈ line 60).
- Remove =(setq elot-missing-dependencies nil)=.  The =defvar= on
  the previous line already initialises it to =nil=.  The
  immediately-following =dolist= that populates the list can stay
  at top level (it does not mutate any external symbol).

** TODO Step 3.2 — Move LaTeX export filter registration into mode lifecycle
- *Files*: =elot-package/elot.el=, =elot-package/elot-mode.el=.
- Remove the top-level
  =(add-to-list 'org-export-filter-item-functions 'elot-latex-filter-omn-item)=
  call (≈ line 159 of =elot.el=).
- In =elot-mode.el=, inside =elot-mode--enable=, add the same
  =add-to-list= call.  Inside =elot-mode--disable=, add the
  matching =(setq org-export-filter-item-functions (delq 'elot-latex-filter-omn-item org-export-filter-item-functions))=.
- Use =add-to-list= so re-enabling does not duplicate.

** TODO Step 3.3 — Verify SPARQL advice lifecycle (done in Milestone 1)
- Milestone 1 Step 1.5 already moved
  =(advice-add 'org-babel-execute:sparql ...)= out of top-level
  scope and into =elot-mode--enable= /
  =elot-mode--disable=, with the
  =elot--sparql-advice-installed-p= idempotency guard.
- This step is verification only:
  - =grep -n "advice-add 'org-babel-execute:sparql" elot-package/*.el=
    shows the call only inside =elot-mode--enable=, never at top
    level;
  - the advice fixture from Milestone 1's acceptance check still
    passes.

** TODO Step 3.4 — Move export hook into mode lifecycle
- *Files*: =elot-package/elot.el=, =elot-package/elot-mode.el=.
- Remove the top-level
  =(add-hook 'org-export-before-processing-functions #'elot--prepare-export-buffer)=
  call (≈ line 793 of =elot.el=).
- Inside =elot-mode--enable=, add the same =add-hook=.
- Inside =elot-mode--disable=, add =(remove-hook ...)=.

** TODO Step 3.5 — Same treatment for top-level hooks in =elot-mode.el=
- *File*: =elot-package/elot-mode.el=.
- The three top-level statements at lines ≈843, ≈852, ≈870
  (=advice-add 'xref-find-references...=,
  =add-hook 'xref-after-update-hook ...= twice) install global
  state at =require= time.  Move these into =elot-mode--enable=
  and pair them with =advice-remove= / =remove-hook= in
  =elot-mode--disable=.

** TODO Step 3.6 — Mirror the changes in =elot-defs.org= and =elot-mode.org=
- For every function/hook moved above, find the corresponding
  =#+begin_src= block in =elot-defs.org= or =elot-mode.org= and
  edit it so that re-tangling regenerates the same hand-edited
  =.el= contents.  Tangle and =git diff= must show no further
  changes.

** Acceptance check
- =grep -nE "^\(add-(hook|to-list)|^\(advice-add|^\(setq " elot-package/elot.el elot-package/elot-mode.el= shows only =setq= forms inside =defun=/=let= blocks (none at top level).
- In a fresh Emacs, =(require 'elot-mode)= followed by
  =(advice--p (advice--symbol-function 'org-babel-execute:sparql))=
  returns =nil= (no advice installed).  After =M-x elot-mode= in
  an org buffer, the same expression returns non-nil.  After =M-x
  elot-mode= a second time (disable), it returns =nil= again.
- Run the [[#stable-status][stable-status checklist]].

* Milestone 4 — Pick a single =MAIN FILE= and fix =Package-Requires=
:PROPERTIES:
:CUSTOM_ID: milestone-4-package-requires
:END:

** Background

A multi-file MELPA package needs exactly one file that carries the
authoritative =Package-Requires= header.  That same file is what
the recipe's =:files= clause considers the "main" library.  ELOT
currently has three files declaring =Package-Requires= (=elot.el=,
=elot-mode.el=, =elot-flymake.el=), and =elot.el= lists =ht= which
is no longer used.

** Decision: =elot.el= is the MAIN FILE
- The MELPA package is named =elot= and the recipe will be
  =recipes/elot=.  MELPA infers the main library from the recipe
  name, so =elot.el= must carry the canonical headers.
- =elot.el= keeps the authoritative =Package-Requires=,
  =Version:=, =URL:=, and =Commentary:= headers.
- =elot-mode.el= remains the user-visible *minor mode* (it defines
  =elot-mode= and its keymap) but it is no longer the load entry
  point: it is ~require~d from =elot.el=.  It does *not* carry its
  own =Package-Requires=.

** TODO Step 4.1 — Make =elot.el= carry the canonical =Package-Requires=
- *File*: =elot-package/elot.el=.
- Ensure the header block contains, in this order:
  : ;; Version: 2.0.0
  : ;; URL: https://github.com/johanwk/elot
  : ;; Package-Requires: ((emacs "29.1"))
- The =Package-Requires= value is the union of all transitive
  runtime dependencies — minus =ht= (unused) and minus packages
  that are loaded with =(require ... nil t)= (i.e., truly
  optional).  After auditing (Step 4.2) the actual list is
  expected to remain =((emacs "29.1"))= because every external
  package is in fact optional.  Confirm or revise as audit
  dictates.

** TODO Step 4.2 — Audit each external package and decide hard vs optional
For each of =htmlize=, =omn-mode=, =hydra=, =sparql-mode=,
=ob-sparql=, =ob-plantuml=, =ht=:

1. =git grep -n "(require '<pkg>"= and =git grep -n "<pkg>-"=
   inside =elot-package/=.
2. Classify:
   - *Hard* — code paths that always run on mode enable will
     fail without the package.
   - *Optional* — only used inside =(when (fboundp ...))= or
     =(require ... nil t)= guards.
3. *Hard* packages go into =Package-Requires=; *optional* ones
   are documented in =elot.el='s commentary instead.

Expected outcome (from current code reading): all seven are
optional except =ht=, which is unused and must be removed.

** TODO Step 4.3 — Remove =Package-Requires= from sub-files
- *Files*: =elot-package/elot-mode.el=, =elot-package/elot-flymake.el=.
- Delete the =;; Package-Requires:= line from each.  The MAIN FILE
  (=elot.el=) now owns this.

** TODO Step 4.4 — Drop the unused =ht= dependency in =elot.el=
- *File*: =elot-package/elot.el=.
- Remove =ht= from the =dolist= at ≈ line 62.
- Remove =ht= from any leftover docstrings or commentary.
- Confirm with =git grep -n "(require 'ht\\b\\|ht-"
  elot-package/= that nothing references it.

** TODO Step 4.5 — Update =elot-defs.org=
- *File*: =elot-defs.org=.
- Sync the =Package-Requires= block in the literate source with
  the changes above so re-tangling does not undo them.

** TODO Step 4.6 — Make =elot.el= the canonical load entry point
- *File*: =elot-package/elot.el= (and =elot-defs.org= mirror).
- Near the bottom of =elot.el= (just before =(provide 'elot)=),
  add ~require~ forms for every sibling module that should be
  available after =(require 'elot)=:
  : (require 'elot-mode)
  : (require 'elot-tangle)
  : (require 'elot-label-display)
  : (require 'elot-flymake)
  : (require 'elot-owl-grammar)
  Modules that are themselves optional (e.g. =elot-db=,
  =elot-sources=) should be loaded with =(require 'elot-db nil t)=
  so a missing optional module does not break the main load.
- Confirm with a fresh Emacs:
  #+begin_src sh
  emacs --batch -L elot-package \
    --eval "(require 'elot)" \
    --eval "(message \"elot-mode bound: %s\" (fboundp 'elot-mode))"
  #+end_src
  Output must include =elot-mode bound: t=.
- Update =make smoke= in the top-level =Makefile= so the
  stable-check now exercises =(require 'elot)= instead of
  =(require 'elot-mode)=.

** Acceptance check
- =grep -n "^;; Package-Requires:" elot-package/*.el= shows
  exactly one match (=elot.el=).
- =git grep -n "\\bht\\b" elot-package/= returns no Elisp matches
  (string literals in fixtures are fine).
- =(require 'elot)= in a fresh Emacs makes =elot-mode= available
  without any further =require= calls.
- Run the [[#stable-status][stable-status checklist]].

* Milestone 5 — Standardise file headers and licenses
:PROPERTIES:
:CUSTOM_ID: milestone-5-headers
:END:

** Background

MELPA enforces standard file headers via =package-lint=.  Three
files do not meet the bar: =elot-owl-grammar.el= has no copyright
or license at all; =elot-db.el= and =elot-sources.el= use MIT
without GPL boilerplate and miss =Version:=/=URL:= headers.

** TODO Step 5.1 — Add a full GPL-3 header to =elot-owl-grammar.el=
- *File*: =elot-package/elot-owl-grammar.el=.
- Insert immediately after the =;;; ... -*- lexical-binding: t; -*-=
  line: copyright, author, URL, Keywords, GPL-3 license boilerplate,
  =Commentary:=, =Code:= — modelled on the header of
  =elot-mode.el=.  Keep the existing "Generated by …" comment
  inside =;;; Commentary:=.

** TODO Step 5.2 — Standardise =elot-db.el= and =elot-sources.el= on GPL-3
- *Files*: =elot-package/elot-db.el=, =elot-package/elot-sources.el=.
- Replace the =SPDX-License-Identifier: MIT= line with the full
  GPL-3 boilerplate used by =elot-mode.el= (also covered by the
  repo's =LICENSE= file).
- Add =Version: 2.0.0= and =URL: https://github.com/johanwk/elot=
  to match the rest of the package.
- Update the =Copyright= line to =2024, 2025, 2026 The ELOT authors=
  for consistency.

** TODO Step 5.3 — Add =:group= to every =defcustom= and =defgroup= where missing
- =defgroup elot-tangle= and =defgroup elot-label-display= do not
  exist; their =defcustom= entries are not assigned a group, which
  =checkdoc= flags.
- For each file, ensure either an existing =defgroup= covers all
  its =defcustom= s, or add a sub-group:
  - =elot-tangle.el= → add =(defgroup elot-tangle nil ... :group 'elot)=.
  - =elot-label-display.el= → add =(defgroup elot-label-display nil ... :group 'elot)=.
  - =elot-mode.el= → confirm all use =:group 'elot=.
- Add =:group= to every =defcustom= in those files.

** TODO Step 5.4 — Re-evaluate the =no-native-compile= flag in =elot.el=
- *File*: =elot-package/elot.el=.
- After Milestone 2 the function that needed
  =org-link-abbrev-alist-local= access at native-compile time
  has moved to =elot-tangle.el=.  Try removing the
  =no-native-compile: t;= cookie and run:
  #+begin_src sh
  emacs --batch -L elot-package \
    --eval '(setq native-comp-async-report-warnings-errors nil)' \
    -f batch-native-compile elot-package/elot.el
  #+end_src
- If native compilation succeeds and =elot-mode= still works in
  an example file, drop the cookie.  Otherwise leave it and add a
  one-line comment naming the function that requires it.

** Acceptance check
- =package-lint= via:
  #+begin_src sh
  emacs --batch \
    --eval "(require 'package)" \
    --eval "(add-to-list 'package-archives '(\"melpa\" . \"https://melpa.org/packages/\") t)" \
    --eval "(package-initialize)" \
    --eval "(unless (package-installed-p 'package-lint) (package-refresh-contents) (package-install 'package-lint))" \
    --eval "(setq package-lint-main-file \"elot-package/elot.el\")" \
    -L elot-package -f package-lint-batch-and-exit elot-package/*.el
  #+end_src
  Returns no errors.  Warnings noted for follow-up but do not block.
- =checkdoc= via =M-x checkdoc-file= on each =elot-package/*.el=
  reports no new warnings vs the baseline (Step B.1).
- Run the [[#stable-status][stable-status checklist]].

* Milestone 6 — CI workflow for tests and lint
:PROPERTIES:
:CUSTOM_ID: milestone-6-ci
:END:

** Background

The repository currently has only one GitHub Actions workflow
(README auto-update).  MELPA's own CI runs =package-build= and
=package-lint=, so if those fail the submission is blocked.  A
local CI workflow that runs both, plus the ERT suite, catches
problems before submission.

** TODO Step 6.1 — Add =.github/workflows/test.yml=
- *File*: =.github/workflows/test.yml= (new).
- Trigger on =push= to =main= and on =pull_request=.
- Matrix on Emacs versions: =29.1=, =29.4=, =30.1= (snapshot if
  available), Linux only.
- Steps:
  1. =purcell/setup-emacs= action.
  2. =actions/checkout= action.
  3. =make -C test=.
  4. Byte-compile =elot-package/*.el= with =-Werror=.

** TODO Step 6.2 — Add =.github/workflows/lint.yml=
- *File*: =.github/workflows/lint.yml= (new).
- Same trigger conditions.
- Run =package-lint= against =elot-package/elot.el= as the
  MAIN FILE (=package-lint= follows =require= s and lints the
  whole package transitively from the main file).
- Run =checkdoc-file= against every =elot-package/*.el=.
- Both jobs must be /required status checks/ (configure once
  the workflow is green).

** TODO Step 6.3 — Mark ROBOT-gated tests skipped on CI
- Confirm that tests requiring ROBOT call =skip-unless= or the
  equivalent so they pass-skip on Linux runners without ROBOT.
- The =test/Makefile= comments suggest this is already in place
  for some tests; verify all relevant ones — including any new
  SPARQL tests added in Milestone 1.

** Acceptance check
- =git push= triggers both workflows and they go green on the
  feature branch.
- Run the [[#stable-status][stable-status checklist]] locally.

* Milestone 7 — MELPA recipe and submission
:PROPERTIES:
:CUSTOM_ID: milestone-7-recipe
:END:

** Background

A MELPA recipe is a single Elisp form in the =melpa/melpa=
repository under =recipes/elot=.  A draft is sketched in
=ELOT-DB-PLAN.org= but has not been finalised.

** TODO Step 7.1 — Finalise the recipe form
- Working file (this repo): create =elot-package/MELPA-RECIPE=.
- Contents (verify each path glob actually matches files in
  =elot-package/=):
  #+begin_src emacs-lisp
  (elot
   :fetcher github
   :repo "johanwk/elot"
   :files ("elot-package/*.el"
           "elot-package/schema.sql"
           "elot-package/elot-lob.org"
           "elot-package/*.css"
           "elot-package/*.js"
           "elot-package/*.setup"))
  #+end_src
- If Milestone 2 Step 2.5 ended up deleting =elot-lob.org= rather
  than retaining it inert, drop that line from =:files=.
- Confirm the MAIN FILE is =elot.el= (Milestone 4).  Since the
  recipe is named =elot=, MELPA picks =elot.el= as the main
  library by matching =<recipe>.el= — which is exactly what we
  want.  No rename is needed.  The decision (=elot.el= as MAIN
  FILE, package name =elot=) is recorded in the Decisions log
  below and should also be noted under "Design Decisions" in
  =REFACTORING.org=.

** TODO Step 7.2 — Local recipe build smoke test
- Use =package-build= locally:
  #+begin_src sh
  git clone https://github.com/melpa/melpa.git /tmp/melpa
  cp elot-package/MELPA-RECIPE /tmp/melpa/recipes/elot
  cd /tmp/melpa && make recipes/elot
  #+end_src
- =make recipes/elot= produces a =.tar= under =/tmp/melpa/packages/=.
- Install that tarball into a clean Emacs profile:
  =M-x package-install-file=, then =M-x elot-mode= in an example
  =.org=.  Confirm it activates.

** TODO Step 7.3 — Update README installation section
- *Files*: =README.org=, =README.md= (auto-generated).
- Add a "Install from MELPA" subsection ahead of the existing
  "clone from Git" instructions.  Mark "from Git" as the
  development-track option.

** TODO Step 7.4 — Open the MELPA pull request
- Fork =melpa/melpa= on GitHub.
- Add =recipes/elot= with the contents from Step 7.1.
- Open the PR.  Address reviewer feedback in this repo (not in
  the recipe PR) by pushing fixes to =main= and asking the
  reviewer to re-test.
- Track the PR number in =REFACTORING.org=.

** Acceptance check
- =/tmp/melpa/packages/elot-*.tar= installs cleanly in a fresh
  Emacs profile and =elot-mode= works.
- The MELPA PR is open and CI on it is green.

* Post-submission backlog (out of scope until MELPA accepts)

These are tracked in =REFACTORING.org= already and only listed
here as a reminder:

- Phase 2: ERT regression tests for every function in the tangle
  pipeline (REFACTORING.org).
- Phase 3: improved docstrings and narrative prose in the literate
  =.org= sources.
- Optional: clean removal of =elot-package/elot-lob.org= and any
  remaining LOB noweb calls in =examples/= once enough time has
  passed for downstream users to update their files.

* Working with this plan in =gptel=

Practical tips for executing this plan with =gptel=:

- *Narrow before sending.*  Place point on a milestone heading and
  =C-x n s= before invoking =gptel-send=; this scopes the prompt
  to the milestone only.
- *Mark steps as you go.*  Each =TODO Step N.M= heading can be
  cycled to =DONE= with =C-c C-t=.  The cumulative count of DONE
  vs TODO under a milestone is your progress indicator
  (=org-columns= can render this as a percentage).
- *Capture command output back into the document.*  After the
  acceptance check for a milestone, dump the relevant =sh= output
  into a =#+begin_example= block under that heading.  This makes
  the file the durable record of "milestone N was green at
  commit SHA X".
- *Commit per step, not per milestone.*  Even though stability is
  defined per milestone, the working tree should be committable
  after each =TODO Step= so a botched LLM edit can be reset
  without losing earlier good work.
- *Refresh the baseline if you stop and resume.*  If many days
  pass between milestones, redo Step B.1 / B.2 to make sure the
  warning/test baseline still reflects =main=.

* Decisions log

| Date       | Decision                                          | Rationale                                  |
|------------+---------------------------------------------------+--------------------------------------------|
| 2026-05-04 | Strategy B (remove and migrate) for legacy LOB pipeline | After the SPARQL rewrite (Milestone 1) no production code path depends on the LOB functions.  Old example files still tangle correctly because the new pipeline silently ignores LOB noweb calls.  The single example with old SPARQL prefix boilerplate (=examples/reasoner-example.org=) is treated as a special case and left untouched.  See Milestone 2. |
| 2026-05-04 | Rewrite SPARQL support before other ELPA tasks    | Subsequent milestones (legacy removal, mode lifecycle, header polish) all touch the SPARQL code; rewriting it first avoids rework.  The rewrite also removes the last production dependency on the legacy LOB prefix block, enabling Strategy B above.  See Milestone 1. |
| YYYY-MM-DD | =elot.el= chosen as MAIN FILE; package name =elot= | Matches MELPA recipe-name → main-file convention; avoids renaming the user-facing package; =elot-mode.el= remains the autoloaded entry point loaded from =elot.el=. See Milestones 4 and 7. |
| YYYY-MM-DD | Drop =no-native-compile= in =elot.el=             | (fill in — see Milestone 5 Step 5.4)       |
| YYYY-MM-DD | Recipe filename =recipes/elot=                    | Package name is =elot=; MAIN FILE is =elot.el=. See Milestone 7 Step 7.1. |
